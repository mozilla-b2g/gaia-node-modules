"use strict";

module.exports = detectOS;

function detectOS() {
  var target = arguments[0] === undefined ? process : arguments[0];

  var platform = target.platform;
  switch (platform) {
    case "darwin":
      return "mac64";
    case "linux":
      var arch = target.arch;
      return arch === "x64" ? "linux-x86_64" : "linux-i686";
    case "win32":
      return "win32";
  }
}
//# sourceMappingURL=detectos.js.map